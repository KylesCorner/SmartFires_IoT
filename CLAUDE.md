# SmartFires IoT compatibility note

The canonical agent instructions have moved to [`AGENTS.md`](AGENTS.md). Read that file and [`documentation/README.md`](documentation/README.md) before changing this repository.

Keep project facts in `AGENTS.md` and the current documentation rather than duplicating them here; this small compatibility file exists only for tools that automatically look for `CLAUDE.md`.
