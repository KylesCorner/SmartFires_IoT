# Edge Unit

Jetson base-station ingest now lives in `edge/edge-reciever`.

- Run receiver: `smartfires-edge receive ...` (after `pip install -e edge/edge-reciever`)
- Standalone anemometer check remains in `edge/anemometer_read.py`
